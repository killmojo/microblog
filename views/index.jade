include header

	if  !user
		.hero-unit
			h1 欢迎来到 Microblog
			p Microblog 是一个基于 Node.js 的微博系统。
			p 
				a.btn.btn-primary(href="/login") 登陆
				a.btn.btn-default.btn-large(href="/reg") 立即注册
	else
		include say
	include posts

include footer